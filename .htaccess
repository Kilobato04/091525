# ===== ENHANCED CASH FLOW ANALYSIS TOOL - APACHE CONFIGURATION =====
# Version 2.0.0 - Optimized for Real-time Features and Performance

# Enable rewrite engine
RewriteEngine On

# ===== CRITICAL: FORCE CORRECT MIME TYPES =====
<IfModule mod_mime.c>
    # Core web files
    AddType text/css .css
    AddType application/javascript .js
    AddType text/html .html
    AddType application/json .json
    AddType application/xml .xml
    
    # Web fonts
    AddType application/font-woff2 .woff2
    AddType application/font-woff .woff
    AddType application/vnd.ms-fontobject .eot
    AddType font/truetype .ttf
    AddType font/opentype .otf
    
    # Images
    AddType image/svg+xml .svg .svgz
    AddType image/webp .webp
    AddType image/avif .avif
    AddType image/png .png
    AddType image/jpeg .jpg .jpeg
    AddType image/gif .gif
    AddType image/x-icon .ico
    
    # Data files
    AddType text/csv .csv
    AddType application/pdf .pdf
    
    # Force UTF-8 encoding for text files
    AddCharset UTF-8 .html .css .js .json .xml .csv .txt
</IfModule>

# ===== ENHANCED HEADERS FOR REAL-TIME FEATURES =====
<IfModule mod_headers.c>
    # Force CSS content type with UTF-8
    <FilesMatch "\.css$">
        Header set Content-Type "text/css; charset=utf-8"
        Header set Cache-Control "public, max-age=31536000"
    </FilesMatch>
    
    # Force JS content type with UTF-8
    <FilesMatch "\.js$">
        Header set Content-Type "application/javascript; charset=utf-8"
        Header set Cache-Control "public, max-age=31536000"
    </FilesMatch>
    
    # HTML files - no cache for dynamic content
    <FilesMatch "\.html$">
        Header set Content-Type "text/html; charset=utf-8"
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </FilesMatch>
    
    # JSON files for data export/import
    <FilesMatch "\.json$">
        Header set Content-Type "application/json; charset=utf-8"
        Header set Cache-Control "no-cache, no-store, must-revalidate"
    </FilesMatch>
    
    # Security headers
    Header always set X-Content-Type-Options nosniff
    Header always set X-Frame-Options SAMEORIGIN
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
    
    # Enhanced security for sensitive data
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self' data:; connect-src 'self'"
    
    # Remove server signature for security
    Header unset Server
    Header always unset X-Powered-By
    Header unset X-CF-Powered-By
    
    # CORS headers for API compatibility
    Header always set Access-Control-Allow-Origin "*"
    Header always set Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE"
    Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
</IfModule>

# ===== ENHANCED COMPRESSION =====
<IfModule mod_deflate.c>
    # Compress text-based files
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/json
    AddOutputFilterByType DEFLATE image/svg+xml
    
    # Don't compress already compressed files
    SetEnvIfNoCase Request_URI \
        \.(?:gif|jpe?g|png|zip|gz|tar|bz2|sit|rar|pdf)$ no-gzip dont-vary
    SetEnvIfNoCase Request_URI \
        \.(?:exe|t?gz|zip|bz2|sit|rar)$ no-gzip dont-vary
</IfModule>

# ===== OPTIMIZED BROWSER CACHING =====
<IfModule mod_expires.c>
    ExpiresActive on
    
    # CSS and JavaScript - long cache for versioned files
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    
    # HTML - no cache for real-time updates
    ExpiresByType text/html "access plus 0 seconds"
    
    # Images - long cache
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/avif "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    
    # Fonts
    ExpiresByType application/font-woff2 "access plus 1 year"
    ExpiresByType application/font-woff "access plus 1 year"
    ExpiresByType font/truetype "access plus 1 year"
    ExpiresByType font/opentype "access plus 1 year"
    
    # Data files - no cache for exports
    ExpiresByType application/json "access plus 0 seconds"
    ExpiresByType text/csv "access plus 0 seconds"
    
    # Default
    ExpiresDefault "access plus 1 month"
</IfModule>

# ===== ENHANCED ERROR PAGES =====
ErrorDocument 404 /scn/091525/index.html
ErrorDocument 403 /scn/091525/index.html
ErrorDocument 500 /scn/091525/index.html
ErrorDocument 503 /scn/091525/index.html

# ===== DIRECTORY SECURITY =====
Options -Indexes -ExecCGI -Includes
DirectoryIndex index.html index.htm

# ===== CLEAN URLS FOR SPA-LIKE BEHAVIOR =====
# Remove .html extension from URLs
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# Redirect .html extension to clean URL
RewriteCond %{THE_REQUEST} /([^.]+)\.html [NC]
RewriteRule ^ /%1 [NC,L,R=301]

# ===== ENHANCED FILE PROTECTION =====
<FilesMatch "\.(htaccess|htpasswd|ini|log|sh|inc|bak|backup|old|orig|save|tmp|temp|swp|swo|conf|cfg|sql|db|sqlite)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Protect sensitive configuration files
<FilesMatch "\.(env|json|yml|yaml)$">
    <RequireAll>
        Require local
    </RequireAll>
</FilesMatch>

# ===== PERFORMANCE OPTIMIZATIONS =====
# Enable Keep-Alive connections
<IfModule mod_headers.c>
    Header set Connection keep-alive
</IfModule>

# Optimize file serving
<IfModule mod_setenvif.c>
    SetEnvIf Request_URI \.(gif|jpg|jpeg|png|webp|avif|svg|css|js|woff|woff2|ttf|otf|eot|ico)$ no-etag
</IfModule>

# Remove ETags for better caching
FileETag None

# ===== FORCE UTF-8 ENCODING =====
AddDefaultCharset UTF-8

# ===== ENHANCED FILE PERMISSIONS =====
<Files "*.css">
    Order allow,deny
    Allow from all
    Require all granted
</Files>

<Files "*.js">
    Order allow,deny
    Allow from all
    Require all granted
</Files>

<Files "*.html">
    Order allow,deny
    Allow from all
    Require all granted
</Files>

<Files "*.json">
    Order allow,deny
    Allow from all
    Require all granted
</Files>

# ===== MOBILE OPTIMIZATION =====
<IfModule mod_setenvif.c>
    # Detect mobile devices
    SetEnvIf User-Agent "Mobile" mobile
    SetEnvIf User-Agent "Android" mobile
    SetEnvIf User-Agent "iPhone" mobile
    SetEnvIf User-Agent "iPad" mobile
</IfModule>

# ===== API ENDPOINT SIMULATION =====
# Handle AJAX requests properly
<IfModule mod_rewrite.c>
    RewriteCond %{REQUEST_METHOD} OPTIONS
    RewriteRule ^(.*)$ $1 [R=200,L]
</IfModule>

# ===== MONITORING AND LOGGING =====
<IfModule mod_log_config.c>
    # Custom log format for performance monitoring
    LogFormat "%h %l %u %t \"%r\" %>s %O \"%{Referer}i\" \"%{User-Agent}i\" %D" combined_with_time
</IfModule>

# ===== BACKUP AND RECOVERY =====
# Protect backup files
<FilesMatch "\.(backup|bak|save|~)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# ===== DEVELOPMENT VS PRODUCTION =====
# Uncomment for development environment
# Header set X-Environment "development"
# Header set X-Cache-Control "no-cache"

# Uncomment for production environment
Header set X-Environment "production"
Header set X-Optimized "true"

# ===== REDIRECT HTTP TO HTTPS (if SSL available) =====
# Uncomment if you have SSL certificate
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# ===== ENHANCED SECURITY RULES =====
# Block suspicious requests
<IfModule mod_rewrite.c>
    # Block SQL injection attempts
    RewriteCond %{QUERY_STRING} (union|select|insert|delete|drop|create|alter|exec|script) [NC]
    RewriteRule .* - [F,L]
    
    # Block XSS attempts
    RewriteCond %{QUERY_STRING} (<|%3C).*script.*(>|%3E) [NC,OR]
    RewriteCond %{QUERY_STRING} javascript: [NC,OR]
    RewriteCond %{QUERY_STRING} vbscript: [NC]
    RewriteRule .* - [F,L]
    
    # Block directory traversal
    RewriteCond %{QUERY_STRING} (\.\./|\.\.\\) [NC]
    RewriteRule .* - [F,L]
</IfModule>

# ===== FINAL OPTIMIZATIONS =====
# Enable mod_pagespeed if available
<IfModule pagespeed_module>
    ModPagespeed on
    ModPagespeedEnableFilters rewrite_css,rewrite_javascript,rewrite_images
    ModPagespeedEnableFilters collapse_whitespace,remove_comments
</IfModule>

# Custom headers for enhanced features
<IfModule mod_headers.c>
    Header set X-Enhanced-CashFlow "2.0.0"
    Header set X-Features "real-time-editable,dynamic-charts,smart-recommendations"
    Header set X-Last-Updated "2025-06-01"
</IfModule>
